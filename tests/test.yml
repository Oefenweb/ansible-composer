# test file for composer
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - include: pre.yml
  roles:
    - ../../
#  vars:
#    composer_download_url: 'https://getcomposer.org/composer-1.phar'
